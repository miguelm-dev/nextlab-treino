códigos importantes git:
git config --global user.name "nomedousuário" *\**
git config --global user.email emaildousuario \**login global para para usar o git e colocar certinho no github*
git log
git commit -m "mensagem" *\*a "etiqueta", descrição dada pro arquivo*
git add \**para adicionar o arquivo antes de dar push*

git checkout

git push origin main \**para dar o "upload" no github*

git branch



